void main(List<String> args) {
  String name = 'Foo';
  var address = '';

  address = name; // assigning a var to another var of same datatype.
  // invalid operation
  // const age = 20;
  // address = age;
  print(address);
}
